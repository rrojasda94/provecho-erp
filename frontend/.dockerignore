# `node_modules` del host no debe entrar al contexto: la imagen instala el
# suyo con `npm ci` y los binarios nativos (sharp) compilados en Windows no
# corren en Alpine. Copiarlo además rompe el `npm ci` de la capa `deps`.
node_modules
.next
out

# Artefactos de Playwright: se generan al correr la suite, no al construir.
test-results
playwright-report
e2e/.tmp

.env
.env.*
Dockerfile
.dockerignore

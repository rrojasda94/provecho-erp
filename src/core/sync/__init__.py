"""Modo offline del PDV (ADR-009): infraestructura del hub local de
sucursal. `estado_conexion.py` detecta si el hub tiene camino a la nube;
el motor de sync (push/pull vía la API REST existente) es fase 2, ver ADR."""
